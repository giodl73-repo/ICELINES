/*
 * IceLines — htmx.min.js PLACEHOLDER
 * ===================================
 *
 * This is NOT the real HTMX. It is a stub committed by King.1.3 so the
 * static-assets pipeline (MIME, Cache-Control, ETag, include_bytes!)
 * can be tested end-to-end before any HTMX-driven UI lands.
 *
 * To vendor the real HTMX (~14 KB minified) over this stub, run from
 * the workspace root:
 *
 *     bash scripts/vendor-htmx.sh        (Linux / macOS / WSL / Git Bash)
 *     pwsh scripts/vendor-htmx.ps1       (Windows PowerShell)
 *
 * The vendor script downloads HTMX 1.9.x from htmx.org with a SHA-256
 * checksum verification, then overwrites this file. King.2 (the first
 * sub-phase that actually uses HTMX for filter forms) flags a build
 * error if this stub is still in place at compile time.
 *
 * License: BSD-2-Clause (matches the upstream HTMX license).
 */
(function () {
    if (typeof window !== 'undefined' && window.console) {
        console.warn(
            '[icelines] htmx.min.js is a STUB — interactivity disabled. ' +
            'Run scripts/vendor-htmx.sh to install the real HTMX.'
        );
    }
})();
