# FORGE Review - Clear the Unblocks

## Findings

- The proposed pulses are small and compile-safe if they stay docs/test scoped.
- Avoid broad refactors in `headshot.rs`, TUI app state, or shift-profile
  modeling while correcting stale specs.

— forge
