pub async fn run(top: usize, pos: Option<String>, scheme: Option<String>) -> anyhow::Result<()> {
    let _ = (top, pos, scheme);
    println!("icelines rank: not yet implemented in Phase 1 scaffolding");
    Ok(())
}
