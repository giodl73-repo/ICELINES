pub async fn run(team: String, scheme: Option<String>, no_color: bool) -> anyhow::Result<()> {
    let _ = (scheme, no_color);
    println!(
        "icelines team {team}: not yet implemented in Phase 1 scaffolding"
    );
    Ok(())
}
