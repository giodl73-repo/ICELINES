[package]
name    = "icelines-site"
version.workspace = true
edition.workspace = true

[dependencies]
icelines-core = { path = "../icelines-core" }
thiserror.workspace = true
serde.workspace     = true
# Phase 2: tera, toml added here
