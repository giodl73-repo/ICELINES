# Pulse 01: Command Reference and Docs Route Update

Passed. `COMMANDS.md` now says `group create Prospects` is not GET-backed and
points users to `/favorites` POST forms or `icelines group`. The `/docs` route
test checks the rendered embedded docs for that recovery copy.
