# Pulse 01: Command Reference and Docs Route Update

Passed. `COMMANDS.md` now points `watch deployment TOR` to CLI preview or
`/watchlist` player rules, and the `/docs` route test checks the rendered
embedded docs for that recovery copy.
