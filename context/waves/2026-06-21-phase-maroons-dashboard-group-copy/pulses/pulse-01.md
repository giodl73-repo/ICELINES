# Pulse 01: Dashboard Group Copy Update

Passed. The dashboard command parser still rejects group create/delete/rename
and member edits as unsupported GET-backed mutations, and the recovery text now
points users to `/favorites` POST forms or `icelines group`.
